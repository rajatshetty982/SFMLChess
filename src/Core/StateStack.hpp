#include "State.hpp"
#include <memory>
namespace Core {

class StateStack
{

	// typedef std::vector<std::unique_ptr<State>> m_StateStack;
	// should it be that, this contains the vector and and all the methods to traverse and push pop methods here
};
}
